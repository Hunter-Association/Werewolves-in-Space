import React, { useState } from 'react';

// import { GlobalContext } from '../../../store/index';

const GameStateDisplay = ({ height, width, id }) => {
  return (
    <div className="stateDisplay">
    </div>
  );
};

export default GameStateDisplay;